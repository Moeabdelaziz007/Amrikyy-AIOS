// This file will hold the default settings for the OS.

export const DEFAULT_SETTINGS = {
  theme: 'dark',
  language: 'en',
  windowSnap: true,
  soundEffects: true,
  // ... other default settings
};
