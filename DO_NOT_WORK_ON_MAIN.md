تنبيه مهم — لا تعمل على فرع `main`

هذا المستودع الآن يُستخدم بحيث لا يتم إجراء التطوير مباشرة على فرع `main`.
الرجاء اتباع الخطوات التالية للعمل اليومي:

1. أنشئ فرعًا خاصًا بعملك من `develop` أو استخدم الفرع `develop` الموجود:

   git checkout develop
   git pull origin develop
   git checkout -b feat/اسم_الميزة

2. اعمل على فرعك الخاص، ثم افتح طلب سحب (Pull Request) نحو `develop` أو الفرع الذي تحدده الفريق.

3. لا تدمج مباشرة إلى `main` — فقط يتم دمج `main` عبر إجراءات مُحددة أو بعد الموافقة وإصدار إصدار رسمي.

ملاحظة: هذا الملف موجود على فرع `develop` لتذكير المطورين بالسياسة. إذا رغبت بتطبيق حماية إضافية (مثل حماية الفرع في GitHub)، أخبرني وسأرشدك أو أريك الخطوات أو أستخدم صلاحياتك لإعدادها.

شكراً لتعاونك.
